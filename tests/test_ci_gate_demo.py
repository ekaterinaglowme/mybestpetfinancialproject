"""ВРЕМЕННЫЙ намеренно падающий тест — демонстрация, что CI-гейт краснеет.

Цель: показать тимлиду, что job Tests на PR становится красным и (при включённой
branch protection) блокирует мёрж. УДАЛИТЬ (revert этого коммита) после проверки,
до фактического мёржа ветки.
"""


def test_ci_gate_dolzhen_byt_krasnym():
    assert False, "Намеренный фейл: проверяем красный CI-пайплайн (удалить перед мёржем)"
